function solution(seoul) {
    //seoul에서 "Kim" 을 찾는다.
    //찾는다면 "Kim"에 인덱스를 반환해서
    //"김서방은 " + index + "에 있다"와 합침
    for (var i = 0; i < seoul.length; i++){
         if (seoul[i] === "Kim"){    
         return answer = "김서방은 " + i + "에 있다"
         break;
        }
    }

}
